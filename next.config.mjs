/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'upload.wikimedia.org',
            'raw.githubusercontent.com'
        ],
    }
};

export default nextConfig;
